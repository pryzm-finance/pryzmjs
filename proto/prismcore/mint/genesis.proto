syntax = "proto3";
package prismfinance.prismcore.mint;

import "gogoproto/gogo.proto";
import "prismcore/mint/params.proto";
import "prismcore/mint/minter.proto";
// this line is used by starport scaffolding # genesis/proto/import

option go_package = "github.com/prism-finance/prism-core/x/mint/types";

// GenesisState defines the mint module's genesis state.
message GenesisState {
  Params params = 1 [(gogoproto.nullable) = false];

  // minter is an abstraction for holding current rewards information.
  Minter minter = 2 [(gogoproto.nullable) = false];

  // this line is used by starport scaffolding # genesis/proto/state
}
